# Rega Rega (сайт) — HANDOFF для нового чата

> Сайт-платформа Rega Rega (Astro SSG). Точка входа для нового чата: прочитай этот файл.
> Проект: `~/Downloads/rega-batumi/`. Деплой: `npx vercel --prod --yes` (Vercel проект `rega-batumi`, team `bregeadmins-projects`). Live: https://rega-batumi.vercel.app

## ✅ СДЕЛАНО: RU-версия гида (гибрид «вариант A», на проде 2026-06-08)
Выбран гибрид: **UI по-русски, названия/тексты мест остаются английскими** (в `guide_places` нет RU-полей). Перевод контента (вариант B) — отложен (см. следующую задачу).
Что сделано:
- `src/components/GuideCategory.astro` **параметризован языком** (`lang="en"|"ru"`, по умолчанию en — EN-вывод байт-в-байт не изменился). Русские словари: категории (CAT_RU), плитки «ещё из гида» (WMETA_RU), UI-строки (STR.ru). Ссылки через `base` (`/ru/guide`), Google Maps `language=ru`.
- RU-категории — тонкие обёртки над компонентом: `ru/guide/eat-drink.astro` (был фейк-стаб → переписан), **новые** `wellness.astro` / `work.astro` / `move-around.astro`.
- `ru/guide.astro` (хаб) — был фейк (127 мест, unsplash, CSS-карта, битые ссылки) → **переписан data-driven по образцу EN**: реальные счётчики, настоящая Google-карта (#guideMap), реальные новости из `news.json`, импорт общего `guide-hub.css`.
- `ru/guide/news/[slug].astro` — **создан** (зеркало EN news, UI по-русски, контент EN).
- ⚠️ Хэндофф ранее ошибался: `ru/guide/[slug].astro` (страницы мест) **уже были data-driven** — обновил только рендер body (markdown/списки как в EN).

## ✅ СДЕЛАНО: вариант B — перевод описаний мест (на проде 2026-06-08)
Все **52 места** переведены (summary + body) → лежат в **`src/data/guide_ru.json`** (slug → `{summary_ru, body_ru}`). RU-компонент `GuideCategory.astro` и `ru/guide/[slug].astro` накладывают переводы поверх данных из базы (фолбэк на EN). **Без правки схемы базы и без ключа** — переводы в репозитории, версионируются. EN не затронут.
Зачем через файл, а не БД: быстрее, без секретного ключа, всё в репо. Когда дойдут руки до RU-колонок в самой `guide_places` (для админки) — перевод легко перенести из этого файла в БД.
Если правишь/добавляешь места: обнови `guide_ru.json` по slug (markdown: `**жирный**`, `*курсив*`, `* пункт` для списков, короткая `строка:` → лид-заголовок).
Решения Артёма (оба пункта — оставить как есть, НЕ трогать): (1) подкатегории-фильтры (coffee/restaurants/bars…) остаются английскими (теги из БД); (2) часы на RU-странице места — в исходном AM/PM формате (на EN конвертируются в 24ч) — ок. **RU-гид закрыт на 100%.**

## ⏭️ СЛЕДУЮЩАЯ ЗАДАЧА: см. бэклог ниже
RU-гид полностью готов (каркас + переводы). Следующее — из бэклога сайта (переезд на regarega.co / цены квартир / лонгриды).

## ✅ Сделано в этой сессии (всё на проде)
- **Новости:** обложка-плашка на хабе переделана под bre-стиль (дуотон, заголовок-курсив, «read the issue»); страница новости — лид + подпись «by the rega rega team», карточка «the place» (ссылка на место в гиде), мини-галерея.
- **Карта-discoverability на хабе гида:** карта поднята под «миры», панель `all / open now / near me`, плавающая кнопка-пилюля `map`.
- **Airbnb-сплит на страницах категорий гида и на /stays (EN+RU):** список слева + липкая карта справа, синхронизация карточка↔пин, **кластеризация** (markerclusterer), **разворот карты** (в ширину, та же высота, ✕/Esc), **near me** (геолокация), на /stays — **сжатие шапки при скролле** в одну полоску + фильтры в одну кнопку-модалку.
- **Пины-пилюли** (как Airbnb): на /stays цена `₾X` (плейсхолдер — у квартир нет `price_from`); в гиде — название/категория. Движок: `public/rega-map.js` (`priceIcon`/`pick`).
- **Круглые контролы карты** (зум +/− и кнопка-разворот) — в движке + shell.css, на всех картах и языках.
- **Фото мест гида → Supabase Storage** (218 шт, бакет `guide`); сайт независим от Тильды (кроме хвоста ниже).
- **«Что рядом» на квартирах (EN+RU):** реальные ближайшие места из гида (Haversine) вместо демо.
- **Главная (`/`) — data-driven:** блоки гида/новостей из базы, 0 битых ссылок, WhatsApp починён.
- **Галереи** (места + новости): крупные landscape + ведущее фото + **лайтбокс-карусель** (стрелки/клавиши/счётчик + свайп пальцем).
- **Убран кастомный курсор** — везде нативный.
- Цены на карточках/пинах — плейсхолдер `from ₾X` (заполнятся, как проставишь `price_from`).

## ⏳ ОТЛОЖЕНО до задачи, где будет `sb_secret_` ключ
- **Фото новостей → Storage** (`src/data/news.json`, 2 шт, ещё tildacdn). Скрипт готов: `scripts/migrate-news-photos.py`.
- **4 «image-only» места** (urban-coffee, jet-scooter-rent, maxim-taxi, bolt-taxi) — одиночное `image` ещё на tildacdn. Скрипт `scripts/migrate-guide-photos.py` уже умеет их добить — просто перезапустить.
- Способ: положить ключ в env `SUPA_KEY=...`, прогнать скрипт(ы), пересобрать (`npx vercel --prod --yes`), ключ удалить.

## 📋 Бэклог сайта (после RU-гида)
- **🔑 Контент-слой: CMS/админка для текстов** (напомнил Артём 2026-06-08, важно). Идея: редакторские тексты (описания мест EN/RU, новости, лонгриды) править через CMS, а не в коде/БД вручную. Закрывает времянку `src/data/guide_ru.json` (ручной перевод 52 мест) и хардкод-заготовки лонгридов/новостей. Операционка (брони, финансы, квартиры, координаты/часы/категории мест) — в Supabase + Rega OS v2.
  - **Очерёдность (решение Артёма 2026-06-08):** сначала **Directus** (админка), потом **Sanity** (для текстов) — не «или-или», а последовательность; оба будут.
    - **Directus** (Артём вспомнил, что предлагали именно его) — надевается ПОВЕРХ текущей Supabase, контент остаётся в той же базе (`guide_places`), просто даёт админку-редактор; ничего не переносим. Подключаем раньше.
    - **Sanity** — облачная CMS для редакторских текстов, контент в её облаке. Подключаем позже.
  - ⚠️ Скоуп ещё не уточнён (что именно «тексты»: места EN/RU? новости? лонгриды? квартиры?), как делятся роли Directus ↔ Sanity и как CMS ↔ Supabase ↔ сайт. Обсудить дизайн до постройки.
- **🏗 Мульти-город «движок-шаблон» (масштабируемая схема)** — обсуждали 2026-06-08. ⭐ ВАЖНЫЙ факт: данные **уже мульти-город-готовы** — у `guide_places` и `apartments_public` есть колонка `city_id`; контент (тексты/фото/RU) лежит в базе. НО: таблица `cities` **пустая**, сайт `city_id` **не использует** (тянет всё без фильтра по городу), город **не параметр** маршрута, и часть копирайта **вшита в .astro-шаблоны** (заголовки типа «тот Батуми, что советуем», названия районов на карте, описания категорий-«миров»). Итог: «рукодельный» сейчас **только код-обвязка сайта, не контент**.
  - Чтобы стало шаблоном (один движок рендерит любой город): (1) заполнить таблицу `cities`; (2) фильтровать запросы по `city_id` + брать город из адреса (поддомен/путь); (3) вынести вшитые тексты в данные/CMS, чтобы каждый город нёс свои.
  - **Страна = группировка в лобби, НЕ уровень URL** (единица — город, как у Airbnb). НЕ делать `georgia/batumi` сейчас — премачюр. Финальную гео-схему фиксируем на этом этапе (можно с уровнем страны, через редиректы).
  - Тесно связано с контент-слоем (Directus/Sanity) выше — по сути одна большая веха «платформа-шаблон».
- **🌐 Переезд на regarega.co — ✅ ДОМЕН ЖИВОЙ (2026-06-08).** `regarega.co` + `www` → Vercel (проект `rega-batumi`), A `@`→76.76.21.21 и CNAME `www`→cname.vercel-dns.com прописаны в GoDaddy (метод A-записи, nameservers НЕ менялись), SSL выпущен (понадобился передеплой `npx vercel --prod --yes`, чтобы триггернуть). Сейчас apex отдаёт сайт Батуми (Вариант A). Бриф: `~/Downloads/REGA-migration-handoff.md`.
  - **✅ СДЕЛАНО — лобби на главную + Батуми под `/georgia/batumi` (на проде 2026-06-08).** Артём ПЕРЕДУМАЛ: вместо поддомена выбрал **подпапку с гео-иерархией** `/georgia/batumi` (на вырост: `/<country>/<city>`; страна = группировка в лобби, не уровень URL). Всё в ОДНОМ проекте `rega-batumi`:
    - `regarega.co/` = **лобби** (порт из `~/Downloads/rega-rega/src/pages/index.astro` → `src/pages/index.astro`; layout `Base.astro` + `tokens.css` + `public/video/` скопированы). 4 picsum-двери заменены на реальные фото из Storage (botanico/gimlet/sunset/granat); видео-фон Батуми оставлен; двери → `/georgia/batumi/guide`, `/stays`, `guide#news`, `/about`, `/own`. Селектор города batumi-live/tbilisi-soon.
    - Весь сайт Батуми → `src/pages/georgia/batumi/*` (EN) и `src/pages/ru/georgia/batumi/*` (RU, язык-вперёд `/ru/...`). Все внутренние ссылки переписаны на `/georgia/batumi`, относительные импорты пересчитаны по глубине. Переключатель EN↔RU работает без правок (логика `'/ru'+path`).
    - 167 страниц, сборка чистая, 0 осиротевших ссылок. Скрипты переборки: `/tmp/restructure.py`, `/tmp/fix_links.py` (одноразовые).
    - Проект-лобби `rega-rega` (`rega-rega-khaki.vercel.app`) теперь **не используется** (контент перенесён в rega-batumi).
    - ⚠️ Старые корневые URL (`regarega.co/guide`, `/stays`) теперь 404. Сайт новый — не критично; при желании добавить 301-редиректы старое→`/georgia/batumi/*` (через `vercel.json` redirects).
    - ⬜ Проверить на проде: рендер дверей лобби (sticky-скролл) и **Google-карты** на страницах Батуми (ключ привязан к regarega.co — должны работать).
  - **📧 Почта (в работе):** старого владельца Google-MX (5 шт) на домене, доступа нет → сносим и ставим бесплатную пересылку **ImprovMX** → всё на `grinbergartgroup@icloud.com` (адреса `artem@` и `info@`). Артём заводит аккаунт ImprovMX + алиасы (account creation — его); я в GoDaddy меняю MX. ⚠️ Браузер-автоматизация GoDaddy флапает (permission/тайм-ауты) — MX-свап ещё не сделан, старые MX на месте (сайту не мешают). SPF/DKIM TXT на домене нет, только MX.
  - Мелочь: www и apex оба отдают 200 (нет канонического редиректа) — по желанию настроить www→apex редирект для SEO.
- **Цены квартир** — заполнить `price_from` в Supabase → плейсхолдеры `₾X` станут реальными ценами (карточки + пины), без правок кода.
- **Лонгриды** (шаблоны на хабе) + расширить новости — контент.
- **Rega Standard** в карточках — ждёт чек-лист от Артёма.

## Ключевые факты / доступы
- Supabase `qkzinjawtumhjbezlyog`; публичный ключ (чтение): `sb_publishable_R0NeSmTwX9qt0bxlHYj1AA_JIVRqsqU`. Запись — SQL Editor или временный `sb_secret_` ключ (создать → использовать → удалить).
- Google Maps ключ: `AIzaSyAHmVWis6qg53NgzADFrnjHtdBjsxb-Pa4` (referrer-locked: rega-batumi.vercel.app, *.vercel.app, regarega.co, bre.ge). **Карта не рендерится на localhost** — проверять на проде.
- Таблицы: `apartments_public` (вью), `guide_places` (places, RU-полей пока нет). Новости — файл `src/data/news.json`.
- ⚠️ Готча: paste большого SQL в Supabase корёжит не-ASCII → генерить чисто-ASCII (`E'...\uXXXX...'`).
- Превью локально (не из Downloads): сборка `npx astro build` → `cp -R dist/* ~/rega-guide-preview/` → preview-сервер `guide-preview` (port 8790). Карта там не грузится (referrer).
- EN↔RU: макеты `Site.astro` / `SiteRu.astro`; страницы дублируются в `src/pages/` и `src/pages/ru/`.

## Другие хэндоффы
- `~/Desktop/REGA-ROADMAP.md` — общая карта (bre/база/админка). Память: `project_rega_roadmap.md` (там же отложенные дела).
- `~/Downloads/REGA-migration-handoff.md` — переезд на regarega.co.
